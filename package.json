{
  "name": "stentorosaur-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo for the Stentorosaur status monitoring packages (ADR-005)",
  "license": "MIT",
  "author": "Amiable Development <dev@amiable.dev>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/amiable-dev/docusaurus-plugin-stentorosaur.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "test:coverage": "npm run test:coverage --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0"
  }
}
